# Configuration for the sonar-scanner CLI (or the dotnet-sonarscanner MSBuild wrapper).
# Run `dotnet tool install --global dotnet-sonarscanner` once, then see
# IMPLEMENTATION_GUIDE.md Step 6 for the begin/build/end sequence used in CI.

sonar.projectKey=ai-augmented-code-review
sonar.projectName=AI-Augmented Code Review System
sonar.sources=backend/src/CodeReview.Api,backend/src/CodeReview.Core
sonar.tests=backend/src/CodeReview.Tests
sonar.exclusions=**/bin/**,**/obj/**,frontend/node_modules/**,frontend/dist/**
sonar.host.url=http://localhost:9000
